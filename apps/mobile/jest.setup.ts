/**
 * Jest setup (runs via the `setupFiles` array in package.json).
 *
 * The heavyweight environment — React Native mocks, asset and platform
 * transforms — is provided by the `jest-expo` preset. This file carries only
 * app-specific globals; kept intentionally minimal so the suites fail loudly
 * on real regressions rather than on setup noise.
 */
export {};
