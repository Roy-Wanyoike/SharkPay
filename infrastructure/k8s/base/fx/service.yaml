apiVersion: v1
kind: Service
metadata:
  name: fx
  labels:
    app.kubernetes.io/name: fx
    app.kubernetes.io/part-of: sharkpay
    app.kubernetes.io/component: backend
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: fx
  ports:
  - name: http
    port: 8083
    targetPort: http
    protocol: TCP
