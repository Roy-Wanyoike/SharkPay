# EXAMPLE ONLY — copy to <svc>.env (git-ignored) and fill real values.
# Never commit real values; the .env files live outside git.
# Create the in-cluster Secret per service with:
#   kubectl -n sharkpay create secret generic providers-secrets --from-env-file=providers.env
HONEYCOIN_SIGNING_KEY=REPLACE_ME_SIGNING_KEY
HONEYCOIN_CALLBACK_SECRET=REPLACE_ME_CALLBACK_SECRET
