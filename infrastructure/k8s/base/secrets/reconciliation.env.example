# EXAMPLE ONLY — copy to <svc>.env (git-ignored) and fill real values.
# Never commit real values; the .env files live outside git.
# Create the in-cluster Secret per service with:
#   kubectl -n sharkpay create secret generic reconciliation-secrets --from-env-file=reconciliation.env
RECON_DB_URL=jdbc:postgresql://postgres.sharkpay-postgres.svc:5432/sharkpay_recon
RECON_DB_USER=REPLACE_ME
RECON_DB_PASSWORD=REPLACE_ME
