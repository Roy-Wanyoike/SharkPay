# EXAMPLE ONLY — copy to <svc>.env (git-ignored) and fill real values.
# Never commit real values; the .env files live outside git.
# Create the in-cluster Secret per service with:
#   kubectl -n sharkpay create secret generic api-gateway-secrets --from-env-file=api-gateway.env
# api-gateway has no secret env vars today.
# Reserved for upstream OAuth client secrets when the edge
# needs to introspect tokens itself: EDGE_CLIENT_SECRET=REPLACE_ME
