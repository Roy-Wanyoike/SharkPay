apiVersion: v1
kind: Service
metadata:
  name: risk
  labels:
    app.kubernetes.io/name: risk
    app.kubernetes.io/part-of: sharkpay
    app.kubernetes.io/component: backend
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: risk
  ports:
  - name: http
    port: 8084
    targetPort: http
    protocol: TCP
